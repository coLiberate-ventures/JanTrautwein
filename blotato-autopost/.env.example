# Copy to .env for local testing (.env is gitignored — never commit your key).
# In production this value lives as a GitHub Actions secret named BLOTATO_API_KEY.
BLOTATO_API_KEY=your_blotato_api_key_here
